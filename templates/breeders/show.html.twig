{% extends 'base.html.twig' %}

{% block title %}Breeders{% endblock %}

{% block body %}
    <h1>Breeders</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ breeder.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ breeder.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ breeder.email }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ breeder.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('breeders_index') }}">back to list</a>

    <a href="{{ path('breeders_edit', {'id': breeder.id}) }}">edit</a>

    {{ include('breeders/_delete_form.html.twig') }}
{% endblock %}
