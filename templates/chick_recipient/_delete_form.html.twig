<form method="post" action="{{ path('chick_recipient_delete', {'id': chick_recipient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chick_recipient.id) }}">
    <button class="btn">Delete</button>
</form>
