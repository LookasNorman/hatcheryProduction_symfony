{% extends 'base.html.twig' %}

{% block title %}ChickRecipient index{% endblock %}

{% block body %}
    <h1>ChickRecipient index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Email</th>
                <th>PhoneNumber</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for chick_recipient in chick_recipients %}
            <tr>
                <td>{{ chick_recipient.id }}</td>
                <td>{{ chick_recipient.name }}</td>
                <td>{{ chick_recipient.email }}</td>
                <td>{{ chick_recipient.phoneNumber }}</td>
                <td>
                    <a href="{{ path('chick_recipient_show', {'id': chick_recipient.id}) }}">show</a>
                    <a href="{{ path('chick_recipient_edit', {'id': chick_recipient.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('chick_recipient_new') }}">Create new</a>
{% endblock %}
