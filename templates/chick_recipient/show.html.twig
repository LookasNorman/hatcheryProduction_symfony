{% extends 'base.html.twig' %}

{% block title %}ChickRecipient{% endblock %}

{% block body %}
    <h1>ChickRecipient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chick_recipient.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chick_recipient.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ chick_recipient.email }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ chick_recipient.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chick_recipient_index') }}">back to list</a>

    <a href="{{ path('chick_recipient_edit', {'id': chick_recipient.id}) }}">edit</a>

    {{ include('chick_recipient/_delete_form.html.twig') }}
{% endblock %}
