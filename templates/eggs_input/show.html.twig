{% extends 'base.html.twig' %}

{% block title %}EggsInput{% endblock %}

{% block body %}
    <h1>EggsInput</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eggs_input.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eggs_input.name }}</td>
            </tr>
            <tr>
                <th>InputDate</th>
                <td>{{ eggs_input.inputDate ? eggs_input.inputDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eggs_input_index') }}">back to list</a>

    <a href="{{ path('eggs_input_edit', {'id': eggs_input.id}) }}">edit</a>

    {{ include('eggs_input/_delete_form.html.twig') }}
{% endblock %}
