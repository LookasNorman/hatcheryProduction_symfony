{% extends 'base.html.twig' %}

{% block title %}Herds{% endblock %}

{% block body %}
    <h1>Herds</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ herd.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ herd.name }}</td>
            </tr>
            <tr>
                <th>HatchingDate</th>
                <td>{{ herd.hatchingDate ? herd.hatchingDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('herds_index') }}">back to list</a>

    <a href="{{ path('herds_edit', {'id': herd.id}) }}">edit</a>

    {{ include('herds/_delete_form.html.twig') }}
{% endblock %}
