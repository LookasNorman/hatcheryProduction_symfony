{% extends 'base.html.twig' %}

{% block title %}EggsInputDetails{% endblock %}

{% block body %}
    <h1>EggsInputDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eggs_input_detail.id }}</td>
            </tr>
            <tr>
                <th>EggsNumber</th>
                <td>{{ eggs_input_detail.eggsNumber }}</td>
            </tr>
            <tr>
                <th>ChickNumber</th>
                <td>{{ eggs_input_detail.chickNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eggs_input_details_index') }}">back to list</a>

    <a href="{{ path('eggs_input_details_edit', {'id': eggs_input_detail.id}) }}">edit</a>

    {{ include('eggs_input_details/_delete_form.html.twig') }}
{% endblock %}
