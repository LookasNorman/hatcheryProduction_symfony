{% extends 'base.html.twig' %}

{% block title %}EggsInputDetails index{% endblock %}

{% block body %}
    <h1>EggsInputDetails index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>EggsNumber</th>
                <th>ChickNumber</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for eggs_input_detail in eggs_input_details %}
            <tr>
                <td>{{ eggs_input_detail.id }}</td>
                <td>{{ eggs_input_detail.eggsNumber }}</td>
                <td>{{ eggs_input_detail.chickNumber }}</td>
                <td>
                    <a href="{{ path('eggs_input_details_show', {'id': eggs_input_detail.id}) }}">show</a>
                    <a href="{{ path('eggs_input_details_edit', {'id': eggs_input_detail.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('eggs_input_details_new') }}">Create new</a>
{% endblock %}
