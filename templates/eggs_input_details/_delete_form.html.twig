<form method="post" action="{{ path('eggs_input_details_delete', {'id': eggs_input_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eggs_input_detail.id) }}">
    <button class="btn">Delete</button>
</form>
